import styles from './ProfileStore.module.css';

function ProfileStore() {
  return (
    <section className={styles.container}>
      <h2 className={styles.title}>판매 중인 상품</h2>
      {/* 나중에 여기에 상품 목록이 들어옵니다. */}
    </section>
  );
}

export default ProfileStore;
